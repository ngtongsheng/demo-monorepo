export interface User {
  name: string;
  phone: string;
  thumb: string;
  location: string;
}

export interface Photo {
  url: string;
}

export interface CoronaCase {
  id: string;
  cases: number;
}
