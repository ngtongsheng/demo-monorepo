import React from 'react';

import './subtitle.scss';

/* eslint-disable-next-line */
export interface SubtitleProps {}

export const Subtitle = (props: SubtitleProps) => {
  return (
    <div>
      <h1>Welcome to subtitle!</h1>
    </div>
  );
};

export default Subtitle;
